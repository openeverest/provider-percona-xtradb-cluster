{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "provider-percona-xtradb-cluster.fullname" . }}
  labels:
    {{- include "provider-percona-xtradb-cluster.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "provider-percona-xtradb-cluster.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "provider-percona-xtradb-cluster.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
