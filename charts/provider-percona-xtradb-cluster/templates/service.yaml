apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-percona-xtradb-cluster.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "provider-percona-xtradb-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-percona-xtradb-cluster.selectorLabels" . | nindent 4 }}
